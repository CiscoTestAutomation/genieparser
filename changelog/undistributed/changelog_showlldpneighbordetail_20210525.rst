--------------------------------------------------------------------------------
                                Fix
--------------------------------------------------------------------------------
* NX-OS
    * Modified ShowLldpNeighborsDetail:

      * If an NX-OS device is connected to an IOS-XE device with a 10 gig ethernet link
        the output from the parser will return the interface name in a format compatible with IOS-XE